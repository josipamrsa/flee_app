package com.example.fleeapp.domain.model.tracks

import androidx.room.Entity
import androidx.room.PrimaryKey

// Temp class to get rid of error
@Entity
class Temp(
    @PrimaryKey val id: Int? = null
)